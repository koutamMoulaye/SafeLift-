# SafeLift dashboard-v2 -- URL de base de l'API FastAPI EXISTANTE
# (dashboard/main.py). En local via Docker Compose, DASHBOARD_PORT_EXPOSED
# vaut 18000 par defaut (voir .env.example a la racine du projet).
VITE_API_BASE_URL=http://localhost:18000
